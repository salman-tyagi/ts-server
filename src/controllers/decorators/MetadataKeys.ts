/**
 * keys in enum must be start with CAPITAL LETTER but ignored
 */

enum MetadataKeys {
  path = 'path',
  method = 'method',
  middleware = 'middleware',
  validator = 'validator',
}

export default MetadataKeys;
